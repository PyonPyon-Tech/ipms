import { Employee } from "@models/pestcontrol/employee";
import { FC } from "react";

export const EmployeeForm: FC<{data: Employee | null}> = ({data})=>{
    const isAForm =!data
    return <div>
        
    </div>
}